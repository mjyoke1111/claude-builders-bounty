# Sample output: claude-review --pr https://github.com/claude-builders-bounty/claude-builders-bounty/pull/4316

## Summary
Adds a Python pre-tool-use hook (`hooks/block-dangerous-commands.py`) that blocks destructive bash commands (rm -rf, DROP TABLE, git push --force, TRUNCATE, DELETE FROM without WHERE) before Claude Code executes them, with logging to `~/.claude/hooks/blocked.log` and a README covering install in two commands plus settings.json registration.

## Risks
- The `rm -rf` regex requires a path-like character after the flags, so a bare `rm -rf` with trailing space variants could slip through in edge cases.
- Logging failure is silently swallowed; on a read-only home directory blocks would not be recorded, only enforced.
- Blocking happens via exit code 2 semantics; a future Claude Code version changing hook exit conventions would silently disable protection.

## Suggestions
- Add a test matrix script to the repo so regressions in the pattern list are caught in CI.
- Include `git push --force-with-lease` in an allowlist note so users know the safer variant is intentionally not blocked.
- Consider a `--dry-run` env var to log without blocking for evaluation rollouts.

## Confidence
High - the diff is self-contained, the blocking behavior was verifiable from the code plus the documented exit-code contract, and the test evidence is included in the PR description.
